package com.example.bookreviewapp.UI

import androidx.recyclerview.widget.RecyclerView


class BookAdapter(private val ) {

    class BookViewHolder(private val binding: ) : RecyclerView.ViewHolder(binding.root){

    }
}